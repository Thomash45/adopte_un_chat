{% extends 'base.html.twig' %}

{% block title %}Announce{% endblock %}

{% block body %}
    <h1>Announce</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ announce.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ announce.name }}</td>
            </tr>
            <tr>
                <th>Year</th>
                <td>{{ announce.year }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ announce.description }}</td>
            </tr>
            <tr>
                <th>Adresse</th>
                <td>{{ announce.adresse }}</td>
            </tr>
            <tr>
                <th>City</th>
                <td>{{ announce.city }}</td>
            </tr>
            <tr>
                <th>CodePostal</th>
                <td>{{ announce.codePostal }}</td>
            </tr>
            <tr>
                <th>Departement</th>
                <td>{{ announce.departement }}</td>
            </tr>
            <tr>
                <th>Region</th>
                <td>{{ announce.region }}</td>
            </tr>
            <tr>
                <th>TelephoneNumber</th>
                <td>{{ announce.telephoneNumber }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('announce_index') }}">back to list</a>

    <a href="{{ path('announce_edit', {'id': announce.id}) }}">edit</a>

    {{ include('announce_user/_delete_form.html.twig') }}
{% endblock %}