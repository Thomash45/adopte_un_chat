<div class="col-md-8">
<div class="card">
    <header class="card-header">
        <h4 class="card-title mt-2">Poster une annonce</h4>
    </header>
    <article class="card-body">
        {{ form_start(form) }}
        {{ form_errors(form) }}
            <div class="row">
                <div class="col-md-6">
            <div class="form-row">
                <div class="col form-group">
                    {{ form_row(form.name) }}
                </div> <!-- form-group end.// -->
                <div class="col form-group">
                    {{ form_row(form.gender) }}
                </div> <!-- form-group end.// -->
            </div> <!-- form-row end.// -->
            <div class="form-row">
                <div class="col form-group">
                    {{ form_row(form.year) }}
                </div> <!-- form-group end.// -->
                <div class="col form-group">
                    {{ form_row(form.race) }}
                </div> <!-- form-group end.// -->
            </div> <!-- form-row end.// -->
            <div class="form-row">
                <div class="col form-group">
                    {{ form_row(form.coat) }}
                </div> <!-- form-group end.// -->
                <div class="col form-group">
                    {{ form_row(form.coatStyleColor) }}
                </div> <!-- form-group end.// --><div class="col form-group">
                    {{ form_row(form.coatColor) }}
                </div> <!-- form-group end.// -->
            </div> <!-- form-row end.// -->
            <div class="form-group">
                {{ form_row(form.description) }}
            </div>
            <div class="form-row">
                <div class="col form-group">

                    {{ form_row(form.imageFile) }}

                </div> <!-- form-group end.// -->
            </div> <!-- form-row end.// -->
                </div>
                <div class="col-md-6">
                    <div class="form-group">
                        {{ form_row(form.adresse) }}
                    </div> <!-- form-group end.// -->
                    <div class="form-group" id="map">
                    </div> <!-- form-group end.// -->
                </div>
            </div>
    </article> <!-- card-body end .// -->
    <div class="border-top card-body text-center">Have an account? <a href="">Log In</a></div>
</div>
    {{ form_row(form.road) }}
    {{ form_row(form.streetNumber) }}
    {{ form_row(form.city) }}
    {{ form_row(form.codePostal) }}
    {{ form_row(form.departement) }}
    {{ form_row(form.region) }}
    {{ form_row(form.country) }}
    {{ form_row(form.lat) }}
    {{ form_row(form.lng) }}
    <button class="btn">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}

</div>